{
  "name": "web-scrapper",
  "version": "1.0.0",
  "description": "A simple webscrapper for the whosampled.com website",
  "main": "./lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "prepublish": "npm run build",
    "db:create": "sequelize db:create",
    "db:migrate": "sequelize db:migrate"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "@types/node": "^22.5.4",
    "dotenv": "^16.4.5",
    "find-config": "^1.0.0",
    "puppeteer": "^23.3.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "tslint": "^6.1.3",
    "typescript": "^5.5.4",
    "winston": "^3.14.2"
  },
  "files": [
    "./bin/*",
    "./lib/*"
  ],
  "typings": "./lib/index.d.ts",
  "devDependencies": {
    "prisma": "^5.19.1",
    "ts-node": "^10.9.2"
  }
}
